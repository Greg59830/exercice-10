import { ExerciceDirectiveDirective } from './exercice-directive.directive';

describe('ExerciceDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ExerciceDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
